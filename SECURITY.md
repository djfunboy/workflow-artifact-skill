# Security

This repository contains instructions and one static HTML example; it runs no
code and fetches nothing at runtime. If you find something in the skill that
could steer an agent somewhere unsafe, report it privately through GitHub's
"Report a vulnerability" button on the Security tab rather than a public issue.
